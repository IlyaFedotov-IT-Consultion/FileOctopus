[package]
name = "fs-core"
version.workspace = true
edition.workspace = true

[dependencies]
async-trait = "0.1"
chrono = "0.4"
tokio = { version = "1", features = ["rt"] }
vfs = { path = "../vfs" }

[dev-dependencies]
tempfile = "3"
